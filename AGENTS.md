# AGENTS Instructions

- UI theming changes are opt-in only: do not modify styles, templates, resources, or visual theme defaults unless explicitly requested.
- Prefer using existing Fluent theme styles and resource keys; if a visual change seems necessary, ask first.
