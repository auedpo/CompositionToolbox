<Window x:Class="CompositionToolbox.App.Views.Dialogs.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        MinWidth="360">
    <Border Padding="{DynamicResource App.PanelPadding}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="MessageText"
                       Margin="{DynamicResource App.SectionSpacingBottom}"
                       TextWrapping="Wrap" />
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Yes"
                        Width="80"
                        IsDefault="True"
                        Click="Yes_Click"
                        Margin="0,0,6,0" />
                <Button Content="No"
                        Width="80"
                        IsCancel="True" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
