<!-- Purpose: Dialog markup for the Text Prompt Dialog prompt composed in the UI. -->

<Window x:Class="CompositionToolbox.App.Views.Dialogs.TextPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        FocusManager.FocusedElement="{Binding ElementName=InputTextBox}"
        Title="Input"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        MinWidth="360"
        Background="{DynamicResource Brush.App.Background}"
        Foreground="{DynamicResource Brush.Text.Primary}">
    <Border Padding="{DynamicResource App.PanelPadding}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Margin="{DynamicResource App.SectionSpacingBottom}"
                       TextWrapping="Wrap"
                       Text="{Binding Prompt}" />
            <TextBox x:Name="InputTextBox"
                     Grid.Row="1"
                     MinWidth="240"
                     Margin="{DynamicResource App.SectionSpacingBottom}"
                     Text="{Binding ResponseText, UpdateSourceTrigger=PropertyChanged}"
                     GotKeyboardFocus="InputTextBox_GotKeyboardFocus" />
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK"
                        Width="80"
                        IsDefault="True"
                        Click="Ok_Click"
                        Margin="0,0,6,0" />
                <Button Content="Cancel"
                        Width="80"
                        IsCancel="True" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
